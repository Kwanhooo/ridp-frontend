"use client";

import {ModelData} from "@/components/ModelData";

const Page = () => {
    return (
        <div className="p-2 h-full">
            <ModelData/>
        </div>
    );
};

export default Page;
